{
  "name": "notes-app",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:client": "bun run --cwd apps/client dev",
    "clean": "bun run --cwd apps/* clean && bun run --cwd packages/* clean"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
