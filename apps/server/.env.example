# =================================================================
# Server Configuration
# =================================================================
PORT=8000
HOST=0.0.0.0
NODE_ENV=development

# =================================================================
# Security & Authentication
# =================================================================
# JWT Configuration (min 256 chars recommended)
JWT_SECRET=your_super_secret_key_here_minimum_32_characters_long_recommended_256_characters_long_is_best

# CORS & CSRF Configuration
APP_URL=http://localhost:3000 # Frontend URL for CORS/CSRF origin

# =================================================================
# Database Configuration
# =================================================================
# Database Credentials
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase

# Database Connection
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@localhost:5432/${DB_NAME}

# =================================================================
# Optional Features
# =================================================================
# Email Service (for verification emails)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your_smtp_username
# SMTP_PASS=your_smtp_password
# EMAIL_FROM=noreply@example.com

# Rate Limiting (if implemented)
# RATE_LIMIT_WINDOW=900000 # 15 minutes in ms
# RATE_LIMIT_MAX=100 # max requests per window
