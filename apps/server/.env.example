# Database Configuration
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase

# Connection URL (localhost instead of postgres because local docker compose should be used)
DATABASE_URL=postgresql://myuser:mypassword@localhost:5432/mydatabase

# Server Configuration
PORT=8000
HOST=0.0.0.0
